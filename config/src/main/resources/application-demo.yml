server:
  port: 5100

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:5000/eureka

spring:
  cloud:
    config:
      server:
        git:
          uri: ubuntu@129.204.84.212:/develop/git-repositories/spring-cloud/kotlin-example.git
          default-label: master
          search-paths: config-yml
  security:
    basic:
      enabled: true
    user:
      name: guoguo
      password: guoguo